PORT=5000
JWT_SECRET=subeko223
NODE_ENV=development

# Database Configuration
# Set to 'mysql' to use MySQL, 'sqlite' for SQLite (default)
DB_TYPE=mysql

# MySQL Configuration (only needed if DB_TYPE=mysql)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=user_auth_api